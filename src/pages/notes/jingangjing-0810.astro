---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="次第乞食"
	description="真正的平等，不是情绪上的热心，而是行动里的不分别。"
	keywords="金刚经,次第乞食,读书感悟,阅读笔记"
	canonicalPath="/notes/jingangjing-0810/"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.08.10</div>
			<h1 class="font-huiwen">次第乞食</h1>
			<p class="lead">真正的平等，不是情绪上的热心，而是行动里的不分别。</p>

			<p>《金刚经》开头写佛入舍卫大城乞食，是“次第乞已”。南怀瑾讲这里，会特别提到：不是专挑富人，也不是专挑穷人，而是按次第来。这里面有一种很朴素的修行：不让自己的偏好、同情、期待，悄悄变成分别心。</p>
			<p>我们做事也常这样。喜欢的人，就多给耐心；不喜欢的人，就少一点公正。觉得某件事重要，就认真；觉得琐碎，就随便。表面看是效率，深处可能是心在挑。</p>
			<p>“次第”不是死板，而是一种秩序感。该轮到谁，就处理谁；该完成哪一步，就走哪一步；不因为情绪好坏随意跳过。很多稳定，正是从这种不挑、不乱、不急里长出来的。</p>
			<p>这对工作很实用：先按顺序清掉该清的事，不靠心情决定价值。</p>

			<section class="note-reminder">
				<h2>今日小提醒</h2>
				<p>今天处理任务时，选三件小事按顺序完成。不要先挑最舒服的，也不要逃开最普通的。</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0810-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		margin-bottom: 12px;
		color: #9b7532;
		font-family: var(--font-mono);
		font-size: 0.82rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.note-reminder {
		margin-top: 46px;
		padding: 28px 30px;
		border: 1px solid rgba(139, 106, 43, 0.22);
		border-radius: 2px;
		background: rgba(255, 248, 232, 0.54);

		h2 {
			margin: 0 0 12px;
			color: #8b6a2b;
			font-family: var(--font-mono);
			font-size: 0.9rem;
			letter-spacing: 0.05em;
		}

		p {
			margin: 0;
		}
	}

	@media (max-width: 768px) {
		.note-hero {
			width: calc(100vw - 24px);
			margin: 34px auto 48px;
		}

		.note-content {
			padding: 0 22px;
		}

		.note-reminder {
			padding: 22px;
		}
	}
</style>
