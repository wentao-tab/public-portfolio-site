---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="祇树给孤独园。"
	description="好的学习和修行，需要一个能承载它的场。"
	keywords="金刚经,祇树给孤独园,读书感悟,阅读笔记"
	canonicalPath="/notes/jingangjing-0727/"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.07.27</div>
			<h1 class="font-huiwen">祇树给孤独园。</h1>
			<p class="lead">好的学习和修行，需要一个能承载它的场。</p>

			<p>《金刚经》开头说，佛在舍卫国祇树给孤独园。这个地方不是随便出现的背景。南怀瑾讲到这里，会提到给孤独长者发心建讲堂，祇陀太子也参与成就，所以才有了这个佛经里反复出现的说法场所。</p>
			<p>这件事很实际：道理再高，也需要落地的空间。一个人想学习、写作、修行、工作，如果没有稳定的场，很容易被日常琐碎冲散。场不一定是寺院，也可以是一张桌子、一段固定时间、一个安静角落、一个持续记录的线程。</p>
			<p>祇树给孤独园提醒我们：智慧不是凭空发生的。它需要有人发心，有人护持，有具体地方，有持续秩序。很多成长不是因为某天突然开悟，而是因为你反复回到同一个场，让心慢慢安下来。</p>
			<p>所以，不要只问“我有没有悟性”，也要问“我有没有给理解留一个地方”。</p>

			<section class="note-reminder">
				<h2>今日小提醒</h2>
				<p>今天给自己固定一个小场：十分钟、一张桌子、一页书。只做一件事，不切换，不逃走。</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0727-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		margin-bottom: 12px;
		color: #9b7532;
		font-family: var(--font-mono);
		font-size: 0.82rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.note-reminder {
		margin-top: 46px;
		padding: 28px 30px;
		border: 1px solid rgba(139, 106, 43, 0.22);
		border-radius: 2px;
		background: rgba(255, 248, 232, 0.54);

		h2 {
			margin: 0 0 12px;
			color: #8b6a2b;
			font-family: var(--font-mono);
			font-size: 0.9rem;
			letter-spacing: 0.05em;
		}

		p {
			margin: 0;
		}
	}

	@media (max-width: 768px) {
		.note-hero {
			width: calc(100vw - 24px);
			margin: 34px auto 48px;
		}

		.note-content {
			padding: 0 22px;
		}

		.note-reminder {
			padding: 22px;
		}
	}
</style>
