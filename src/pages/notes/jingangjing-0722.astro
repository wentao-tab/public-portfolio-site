---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="如是我闻。"
	description="真正的学习，先从诚实地承认“我是听来的”开始。"
	keywords="金刚经,如是我闻,读书感悟,阅读笔记"
	canonicalPath="/notes/jingangjing-0722/"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.07.22</div>
			<h1 class="font-huiwen">如是我闻。</h1>
			<p class="lead">真正的学习，先从诚实地承认“我是听来的”开始。</p>

			<p>《金刚经》开头是“如是我闻”。南怀瑾讲这四个字时，会提醒我们：这是阿难对自己记录的负责。他不是说“我发明了这个道理”，也不是说“我已经完全拥有真理”，而是清楚标注：这是我亲自听闻、如实记下的。</p>
			<p>这对今天的学习很有用。我们很容易把听来的观点、读来的概念、别人总结的方法，迅速变成“我的理解”。但中间其实还差几步：听见、记住、复述、验证、内化、应用。少一步都可能只是借来的语言。</p>
			<p>“如是我闻”是一种谦逊，也是一种严谨。它让你先把知识来源放清楚，不急着占有它。读书、听课、看文章、用 AI，都一样：先知道这是信息，不马上把它包装成自己的判断。</p>
			<p>这不是降低自己，而是保护理解的质量。承认“我只是闻”，才有空间继续读第二遍、第三遍、第四遍，直到它真的变成你能使用的东西。</p>

			<section class="note-reminder">
				<h2>今日小提醒</h2>
				<p>今天遇到一个新观点，先标记它的状态：我是刚听到、已理解，还是已经验证过？</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0722-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		margin-bottom: 12px;
		color: #9b7532;
		font-family: var(--font-mono);
		font-size: 0.82rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.note-reminder {
		margin-top: 46px;
		padding: 28px 30px;
		border: 1px solid rgba(139, 106, 43, 0.22);
		border-radius: 2px;
		background: rgba(255, 248, 232, 0.54);

		h2 {
			margin: 0 0 12px;
			color: #8b6a2b;
			font-family: var(--font-mono);
			font-size: 0.9rem;
			letter-spacing: 0.05em;
		}

		p {
			margin: 0;
		}
	}

	@media (max-width: 768px) {
		.note-hero {
			width: calc(100vw - 24px);
			margin: 34px auto 48px;
		}

		.note-content {
			padding: 0 22px;
		}

		.note-reminder {
			padding: 22px;
		}
	}
</style>
