---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="我读金刚经感悟 6/10"
	description="应无所住而生其心。该做的事照做，但不要把心黏在结果上。"
	keywords="金刚经,读书感悟,应无所住而生其心,阅读笔记"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.06.10</div>
			<h1 class="font-huiwen">我读金刚经感悟 6/10</h1>
			<p class="lead">“应无所住而生其心”。</p>
			<p>我今天理解这句话，不是“什么都不要在乎”，而是：该做的事照做，但不要把心黏在结果上。</p>
			<p>人最累的地方，往往不是行动本身，而是行动后面那一串附着：别人怎么看我？这件事有没有证明我？我是不是输给谁了？我这样做值不值？这些念头一多，心就“住”了。</p>
			<p>《金刚经》说“不住”，不是叫人冷漠，而是让人更自由地行动。该努力就努力，该表达就表达，该承担就承担，但不要每一步都拿来加固一个“我”：我的能力、我的面子、我的失败、我的成功。</p>
			<p>真正的“生其心”，反而是在不执着时才生得出来。因为心不被得失绑住，就更清楚；不被评价牵走，就更有力量；不急着证明自己，就更能把事情做好。</p>
			<section class="note-reminder">
				<h2>今天的小提醒</h2>
				<p>做一件该做的小事，做完就放下。不要反复回看自己表现得好不好。让行动完成在行动里。</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0610-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		color: #9b7a35;
		font-family: var(--font-type);
		font-size: 0.82rem;
		letter-spacing: 0.04em;
	}

	.note-reminder {
		margin-top: 44px;
		padding: 22px 0 0 22px;
		border-top: 1px solid rgba(184, 138, 55, 0.22);
		border-left: 3px solid rgba(184, 138, 55, 0.7);

		h2 {
			margin-bottom: 12px;
			color: var(--color-100);
			font-size: 1.28rem;
		}

		p {
			margin-bottom: 0;
		}
	}

	@media(max-width: 767px) {
		.note-hero {
			width: calc(100vw - 32px);
			margin-top: 34px;
			margin-bottom: 48px;
		}

		.note-content {
			padding: 0 24px;

			h1 {
				font-size: 2.45rem;
			}
		}
	}
</style>
