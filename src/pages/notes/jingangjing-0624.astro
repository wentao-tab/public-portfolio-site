---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="知见不生。"
	description="看清一件事以后，不要马上拿它生成一个新的“我”。"
	keywords="金刚经,知见不生,读书感悟,阅读笔记"
	canonicalPath="/notes/jingangjing-0624/"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.06.24</div>
			<h1 class="font-huiwen">知见不生。</h1>
			<p class="lead">看清一件事以后，不要马上拿它生成一个新的“我”。</p>

			<p>人很容易把理解变成身份。读懂一点《金刚经》，就觉得自己比以前通透；看清某段关系，就觉得自己终于醒了；发现一个方法有效，就开始相信“我这一套是对的”。</p>
			<p>这就是知见生了。原本是一个有用的看见，慢慢变成新的执着：我懂、我对、我比别人清楚、我已经不是过去那个我。</p>
			<p>《金刚经》锋利的地方在这里：连正确的见解，也不能住。不是不要见解，而是不要让见解结成自我。否则，知识越多，反而越重；道理越清楚，反而越容易拿来评判别人。</p>
			<p>真正的“知见不生”，是看见了，但心不占有它。这个理解今天能用，就用；明天遇到新情况，也允许它被修正。这样，知识才活。</p>

			<section class="note-reminder">
				<h2>今日小提醒</h2>
				<p>今天当你觉得“我明白了”的时候，补一句：这只是现在的理解，不必拿来证明我是谁。</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0624-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		color: #9b7a35;
		font-family: var(--font-type);
		font-size: 0.82rem;
		letter-spacing: 0.04em;
	}

	.note-reminder {
		margin-top: 44px;
		padding: 22px 0 0 22px;
		border-top: 1px solid rgba(184, 138, 55, 0.22);
		border-left: 3px solid rgba(184, 138, 55, 0.7);

		h2 {
			margin-bottom: 12px;
			color: var(--color-100);
			font-size: 1.28rem;
		}

		p {
			margin-bottom: 0;
		}
	}

	@media(max-width: 767px) {
		.note-hero {
			width: calc(100vw - 32px);
			margin-top: 34px;
			margin-bottom: 48px;
		}

		.note-content {
			padding: 0 24px;

			h1 {
				font-size: 2.45rem;
			}

			p {
				font-size: 1rem;
				line-height: 1.95;
			}
		}
	}
</style>
