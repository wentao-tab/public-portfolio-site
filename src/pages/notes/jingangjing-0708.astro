---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="法身、报身、化身。"
	description="一个人不只看他说什么，还要看他活出来什么、产生什么作用。"
	keywords="金刚经,法身,报身,化身,读书感悟,阅读笔记"
	canonicalPath="/notes/jingangjing-0708/"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.07.08</div>
			<h1 class="font-huiwen">法身、报身、化身。</h1>
			<p class="lead">一个人不只看他说什么，还要看他活出来什么、产生什么作用。</p>

			<p>南怀瑾讲三身，可以粗略对应为：法身是本体，报身是呈现，化身是作用。放到日常，不必讲得玄。你真正相信什么，是法身；你长期训练后显出来的气质、能力、状态，是报身；你对别人、对项目、对世界产生的实际影响，是化身。</p>
			<p>这能帮我们检查自己。很多人有理念，但没有报身：道理懂，身体和行为没跟上。也有人有漂亮呈现，但作用很弱：看起来专业，真正解决问题不多。还有人不太会说，但做出来的东西持续影响别人，那就是作用很实。</p>
			<p>学习也一样。读书后的“法身”是底层理解；“报身”是你表达和判断的变化；“化身”是你能不能把它用到具体事情里。</p>
			<p>所以，不要只问“我懂不懂”，还要问：这份理解有没有改变我的状态？有没有变成行动？有没有真实帮助到一个人或一件事？</p>

			<section class="note-reminder">
				<h2>今日小提醒</h2>
				<p>今天选一个你常说的理念，检查它有没有对应的行为和结果。没有，就先补一个小动作。</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0708-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		margin-bottom: 12px;
		color: #9b7532;
		font-family: var(--font-mono);
		font-size: 0.82rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.note-reminder {
		margin-top: 46px;
		padding: 28px 30px;
		border: 1px solid rgba(139, 106, 43, 0.22);
		border-radius: 2px;
		background: rgba(255, 248, 232, 0.54);

		h2 {
			margin: 0 0 12px;
			color: #8b6a2b;
			font-family: var(--font-mono);
			font-size: 0.9rem;
			letter-spacing: 0.05em;
		}

		p {
			margin: 0;
		}
	}

	@media (max-width: 768px) {
		.note-hero {
			width: calc(100vw - 24px);
			margin: 34px auto 48px;
		}

		.note-content {
			padding: 0 22px;
		}

		.note-reminder {
			padding: 22px;
		}
	}
</style>
