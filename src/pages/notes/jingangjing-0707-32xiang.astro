---
import BlogPostLayout from "../../layouts/BlogPostLayout.astro";
import DetailInteractions from "../../components/DetailInteractions.astro";
import { withBase } from "../../utils/paths";
---

<BlogPostLayout
	title="三十二相。"
	description="外在呈现可以庄严，但不能替代内在真实。"
	keywords="金刚经,三十二相,读书感悟,阅读笔记"
	canonicalPath="/notes/jingangjing-0707-32xiang/"
>
	<main class="reading-note">
		<section class="note-hero">
			<img src={withBase('/assets/cover/header-jingangjing-0610.png')} alt="金刚经的读后感悟" />
		</section>

		<article class="note-content">
			<a class="note-back" href={withBase('/')} aria-label="返回首页">
				<span>←</span>
				返回首页
			</a>
			<div class="note-meta">READING NOTE · 2026.07.07</div>
			<h1 class="font-huiwen">三十二相。</h1>
			<p class="lead">外在呈现可以庄严，但不能替代内在真实。</p>

			<p>《金刚经》里问：可不可以用三十二相见如来？答案是否定的。南怀瑾讲这里，会提醒我们：佛有相，但不能执着相；相可以作为方便，却不是本体。</p>
			<p>放到日常里也一样。一个人的履历、作品、表达、气质、朋友圈、知识体系，都像“三十二相”。它们能显示一些东西，但不能完全代表这个人。看别人如此，看自己也如此。</p>
			<p>我们很容易把“呈现”当成“真实”：页面做得漂亮，就以为产品扎实；话讲得通透，就以为已经做到；读过很多书，就以为真的消化。相有用，但相也会骗人。</p>
			<p>这不是否定外在形式。好的形式、好的表达、好的作品呈现，依然重要。但它们应该服务于真实，而不是遮蔽真实。</p>
			<p>今天可以把“三十二相”当成一个检查：我现在是在打磨真实能力，还是只是在维护一个看起来不错的样子？</p>

			<section class="note-reminder">
				<h2>今日小提醒</h2>
				<p>今天选一件正在展示给别人的东西，问一句：它背后的真实功夫，是否配得上这个呈现？</p>
			</section>
		</article>
		<DetailInteractions
			articleSelector=".note-content"
			storageKey="jingangjing-0707-32xiang-comments"
		/>
	</main>
</BlogPostLayout>

<style lang="scss">
	:global(.train-1) {
		display: none !important;
	}

	.reading-note {
		width: 100%;
		margin: 0 auto 88px;
	}

	.note-hero {
		width: min(1120px, calc(100vw - 48px));
		margin: 56px auto 72px;

		img {
			display: block;
			width: 100%;
			height: auto;
		}
	}

	.note-back {
		display: inline-flex;
		align-items: center;
		gap: 7px;
		margin: 0 0 22px;
		padding: 0;
		border: 0;
		background: transparent;
		color: var(--color-400);
		font-size: 0.86rem;
		line-height: 1;
		text-decoration: none;
		transition: color 0.22s ease, transform 0.22s ease;

		span {
			font-size: 0.95rem;
			transform: translateY(-1px);
		}

		&:hover {
			transform: translateX(-3px);
			color: #8b6a2b;
			text-decoration: none;
		}
	}

	.note-content {
		max-width: 720px;
		margin: 0 auto;
		padding: 0 24px;

		h1 {
			margin: 12px 0 32px;
			font-family: 'Huiwen-mincho', serif !important;
			font-size: clamp(2.6rem, 4.2vw, 4rem);
			font-weight: 400;
			line-height: 1.18;
			color: var(--color-100);
			letter-spacing: 0;
			text-align: left;
		}

		p {
			margin: 0 0 1.35em;
			color: var(--color-200);
			font-size: 1.08rem;
			line-height: 2.05;
		}

		.lead {
			color: #8b6a2b;
			font-family: 'Huiwen-mincho', var(--font-body);
			font-size: clamp(1.55rem, 3vw, 2rem);
			line-height: 1.6;
			margin-bottom: 1.45em;
		}
	}

	.note-meta {
		margin-bottom: 12px;
		color: #9b7532;
		font-family: var(--font-mono);
		font-size: 0.82rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.note-reminder {
		margin-top: 46px;
		padding: 28px 30px;
		border: 1px solid rgba(139, 106, 43, 0.22);
		border-radius: 2px;
		background: rgba(255, 248, 232, 0.54);

		h2 {
			margin: 0 0 12px;
			color: #8b6a2b;
			font-family: var(--font-mono);
			font-size: 0.9rem;
			letter-spacing: 0.05em;
		}

		p {
			margin: 0;
		}
	}

	@media (max-width: 768px) {
		.note-hero {
			width: calc(100vw - 24px);
			margin: 34px auto 48px;
		}

		.note-content {
			padding: 0 22px;
		}

		.note-reminder {
			padding: 22px;
		}
	}
</style>
